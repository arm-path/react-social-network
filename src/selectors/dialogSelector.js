export const getDialogs = (state) => {
    return state.dialogPage.dialogs
}

export const getMessages = (state) => {
    return state.dialogPage.messages
}

export const getTextNewMessage = (state) => {
    return state.dialogPage.textNewMessage
}